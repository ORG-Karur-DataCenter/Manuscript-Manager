name: Sync manuscript tracker

on:
  schedule:
    # Every 3 hours. GitHub may delay scheduled runs slightly at busy times.
    - cron: "0 */3 * * *"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: sync-manuscripts
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - run: npm install --omit=dev

      - name: Run Gmail sync
        env:
          GMAIL_CLIENT_ID: ${{ secrets.GMAIL_CLIENT_ID }}
          GMAIL_CLIENT_SECRET: ${{ secrets.GMAIL_CLIENT_SECRET }}
          GMAIL_REFRESH_TOKEN_SATHISH: ${{ secrets.GMAIL_REFRESH_TOKEN_SATHISH }}
          GMAIL_REFRESH_TOKEN_DHIBIN: ${{ secrets.GMAIL_REFRESH_TOKEN_DHIBIN }}
          # Any one of these is enough; supplying more than one gives the
          # classifier a second opinion and a fallback when a free tier is capped.
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          CEREBRAS_API_KEY: ${{ secrets.CEREBRAS_API_KEY }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          # Pinned to the two Gemini generations: they are verified working, and
          # they cross-check each other. Cerebras currently returns 402
          # (account has no active quota) and the Groq default model id is wrong,
          # so leaving them in the chain would only add failed calls.
          CLASSIFIER_PROVIDERS: gemini-3.7-flash,gemini-2.5-flash
          # Raised for the 30-day first backfill. Anything beyond this is
          # deferred to the next run, not dropped.
          MAX_CLASSIFICATIONS_PER_RUN: "200"
        run: npm run sync

      - name: Commit updated data
        run: |
          git config user.name "manuscript-tracker-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/manuscripts.json data/sync-state.json data/excluded-log.json data/review-queue.json
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Sync manuscript tracker $(date -u +%Y-%m-%dT%H:%M:%SZ)"
            git push
          fi
